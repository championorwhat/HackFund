from django.apps import AppConfig

class FundraiserConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'fundraiser'  # This should match the app folder name 'fundraiser'
